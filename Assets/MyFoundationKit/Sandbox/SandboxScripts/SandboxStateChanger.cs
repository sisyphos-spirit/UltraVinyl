using UnityEngine;

/*public class SandboxStateChanger
{
    //Servicios
    private StateMachine _stateMachine;

    //Estados
    private SandboxStatePlaying _playingState = new SandboxStatePlaying();
    private SandboxStatePause _pauseState = new SandboxStatePause();

    public SandboxStateChanger(StateMachine sm)
    {
        _stateMachine = sm;
    }

    public void Pause()
    {
        _stateMachine.ChangeState(_pauseState);
    }

    public void Play()
    {
        _stateMachine.ChangeState(_playingState);
    }
}*/